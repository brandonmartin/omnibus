--- Makefile.orig	2012-03-24 14:20:54.000000000 -0700
+++ Makefile	2012-03-24 14:56:45.000000000 -0700
@@ -113,6 +113,7 @@
 			  pfx=lib; \
 			  if [ "$(PLATFORM)" != "Cygwin" ]; then \
 				case "$(CFLAGS)" in \
+				*OPENSSL_NO_HW*)	echo  ... skipping \(OPENSSL_NO_HW\); continue;;	\
 				*DSO_BEOS*)	sfx=".so";;	\
 				*DSO_DLFCN*)	sfx=`expr "$(SHLIB_EXT)" : '.*\(\.[a-z][a-z]*\)' \| ".so"`;;	\
 				*DSO_DL*)	sfx=".sl";;	\
